# Vendored upstream (others' work) — see README Attribution; not republished here
dtgwg-cred-spec-main/

# Toolchain + build artifacts (circom lab: see runtimes/circom-gadget/README)
node_modules/
build/
__pycache__/

# Local scratch
.scratch/

# Upstream survey snapshots (public GitHub data, bulky, regenerable via board.mjs survey) — WATCH.md digest is tracked
board/survey/*.json

# Call working directory: pulled ToIP wiki pages (others' content, readable upstream) + private call prep.
# Reviewed and edited here; shared by posting upstream, not by republishing in this repo.
calls/

# Private historical ledger commitments and atomic-write files
.ledger-history/
proverb-ledger.json.lock
proverb-ledger.json.tmp
