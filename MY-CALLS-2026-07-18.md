# My Calls — the decisions and actions that are the First Person's

*2026-07-18. Everything the machine could do is done and green (16 suites / 162 properties; KB built;
briefing v2 drafted). What remains is, by design, yours: circulation, ratification, commits, deploys,
and a handful of genuine design calls. Ordered so you can work top-down; each item says what it
unblocks. Nothing below happens until you act.*

---

## 1 · Circulate (outward-facing — your voice, your send)

- [ ] **The TF briefing** (`BRIEFING-2026-07-18-zkp-explorations.md`, v2 in your panel).
  Read → adjust §3's contribution ordering if you want a different opening with Scott → send to the
  ZKP TF (or hold). *Unblocks: everything in section 2 becoming a group conversation instead of a
  local corpus.* My one flag: §4's log-retention two-faces item is the deliberate open contradiction —
  worth saying aloud on the call rather than in text.
- [ ] **Trust-tasks notes → Glenn Gore** (from the parallel lane: `~/dtgwg-trust-tasks-tf-mage`,
  2 design notes + fit map + PoU draft). Circulate via DTGWG channel, then PR mechanics on your go.
- [ ] **"The First Circuit" chronicle** — decide if it stays an internal chronicle or becomes an
  outward artifact (blog / guide page). It pairs well with the briefing if you circulate both.

## 2 · Ratify or rule (TF-call agenda items — your co-chair seat)

The §28 ten ratification points stand as written in the decision doc. The programme added six:

- [ ] **Transcript-binding public-signal layout** — the fix is applied in the lab
  (`[context, root, nullifier, transcriptDigest]`, +1 constraint); the layout needs the group's
  blessing before any circuit text is proposed upstream.
- [ ] **Semaphore-compat profile: yes/no** — optional second profile (their audited circuit +
  production ceremony, `scope := our descriptor digest`) vs native-gadget-only. My read: cheap to
  offer, real deployment value; the cross-check doc has the one-page mapping sketch.
- [ ] **EdDSA identity: evaluate or defer** — signing-capable enrolment secrets (PR-HLD synergy) at
  the cost of BabyPbk in-circuit. A capability question, not cosmetic. Defer is a legitimate answer
  if v1 scope is tight.
- [ ] **Log retention's clock family** — certification parameter (X5's table) vs erosion effect
  (X6's sort). The record format may need both faces; the group should rule, not the lab.
- [ ] **Fixture format as §26.2** — the strongest exhibit now exists (Python consumer, zero shared
  code, zero divergences). Decide whether to table it with the reason register v2 for ratification.
- [ ] **Enrolment-root id in the nullifier preimage** — currently a public input of the same proof
  but not in the preimage (the gadget's §13.4 partial); ties to §13.3 common-root semantics.

## 3 · Upstream mechanics (only you can push)

- [ ] **Commit the 2-line README edit** in `~/dtgwg-zkp-tf-mage` (the only tracked dirty file —
  VTA capitalization + normative cred-spec link). Smallest item on this list.
- [ ] **E1–E7** → GitHub issues/PRs on the strawman, after (or with) the briefing lands.
- [ ] **Decision doc §31 promotion** — when you call it review-ready, it moves to the workbench at
  `docs/implementation-guide/boundaries/` as a PR-shaped tracked change (WORKFLOW lane 2→3; the
  first full pass through the promote lane).

## 4 · The knowledge base — pick its home

The KB is built and portable (`kb/` — 76 pages, both carriers, deterministic, gated). One choice:

- [ ] **Destination:** (a) FedWiki farm — drop `kb/fedwiki/` pages into a farm host (a new
  `dtg.localhost` sibling? then the guide federation via the normal resync→gate→deploy train);
  (b) git — commit `kb/markdown/` to a repo (private first, per your gated-rollout habit);
  (c) Confluence — import the markdown bundle (ToIP has Confluence; this would put the corpus where
  the WG already lives — worth weighing against how draft-stage the content is);
  (d) hold — rebuild stays one command whenever you decide.
  My read: (a) for your own working view now, (c) only after the briefing lands with the group.
- [x] ~~If (a): wire the farm host~~ **DONE 2026-07-18** — `dtg.localhost` seated as the 10th
  federation site (77 pages, hub + family + audit wired, registry rows in both skill copies; 2
  leak-scan hits = the manifested chronicles, false positives). Start the farm to browse it:
  `wiki --data ~/.wiki --port 3030 --farm` (absolute wiki path). Public deploy stays your G-M gate.

## 5 · Repo hygiene (the standing G-M ledger, still open from 07-16/17)

- [ ] `cd ~/agentprivacy_master && node scripts/mirror-conjecture-register.mjs` (classifier blocked
  me; one command) — then review + commit master.
- [ ] Review + commit the other working trees: spellweb, myterms, dual-agent-harness,
  agentprivacy-docs (plans + register-head line), `~/.wiki` (rebuilt pages + build-harness sealing),
  agentprivacy.guide (site/), dtgwg-zkp-tf README (item in §3), **plus today's**: the research root
  (`~/dtgwg-cred-spec-main_mage` — has no git yet; decide if it gets its own local repo now that it
  holds the decision doc, briefing, explorations, and KB), and the chronicle in master.
- [ ] `cd ~/agentprivacy.guide && npx wrangler deploy` (the two snapshot trains are gated and
  waiting).
- [ ] **Rotate the FedWiki friend secret** (leaked value still live in
  `~/.claude/skills/agentprivacy-wiki-sync/SKILL.md`) — flagged 07-16, still open.
- [ ] Delete the `.bak-2026-07-10` stray in `privacy-layer/agentprivacy-uor-toroidal/`.

## 6 · Build gates you should open deliberately (not urgency — design judgment)

- [x] ~~Guardian t-of-n circuit~~ **DONE 2026-07-18** on your go — GuardianThreshold(20, 3), 8/8,
  16,078 constraints (3 legs + 3 distinctness + 1 claim binding); duplicate seat unsatisfiable;
  one named residue for the promote lane: reconciling the reference model's flat set-commitment
  with the circuit's Merkle root. t=4 at depth 20 needs a bigger ptau (pot15).
- [ ] **Production trusted setup** — the moment anything circuit-shaped leaves the lab, the lab-only
  ceremony becomes a blocker; a real MPC ceremony (or adopting Semaphore's artifacts via the compat
  profile) is a decision that shapes item 2's compat question.
- [ ] **G-DOCS Gaps 1–5** routing with the docs agent (canon fixes via pipeline) — the last
  cred-spec coherence leftovers.
- [ ] The five **sealed harness files** — promote any to circulating, or leave sealed.

---

*Working rhythm suggestion: 1 → 3 (the two sends + one commit are an afternoon), then 4's destination
call, then 2 lands naturally at the next TF call, then 5 as one hygiene sweep. Section 6 has no
clock.*
