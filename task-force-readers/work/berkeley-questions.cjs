module.exports=function(questions){
const add=(id,text)=>{questions.find(q=>q.id===id).ask+=' '+text};
add('Q09','For a blinded per-exchange identifier, are the paper’s PRF-derived per-issuer/per-context pseudonyms with in-proof consistency the right mechanism, or do exchange binding and context pseudonymity need separate constructions? Which parameters and reuse patterns create correlators?');
add('Q10','Can the predicate enforce act ∈ scope_n ⊆ … ⊆ scope_root, bounded chain depth, monotone expiry, revocation non-membership and root membership in registry R? Is this a better-defined initial target than proving task completion?');
add('Q07','The shared note describes purpose-and-governance-bounded contexts and issuer–verifier collusion resistance as adopted targets. Against the exact impossibility result, what refinements are needed for that extended profile, and which adversary and time horizon can evidence support?');
add('Q21','The shared note reports a prototype sampling about 29 of several thousand credentials, with a 95% confidence / 10% bad-fraction target and roughly 7 seconds proving time. What precise experiment and probability statement would validate those reported figures? Does the paper’s multi-issuer confidence result support this aggregation, or rely on different assumptions? What changes when issuers are correlated rather than independent, and how does the sampling proof compose with personhood and relationship show proofs?');
add('Q01','What is the smallest end-to-end creation and presentation of an open verifiable-trust-community credential that exercises the framework’s essential bindings and is representative rather than a toy?');
const extra=[
['Q23','Can the proof cover exchange artifacts as well as credentials?','Berkeley 1 · Trust Task artifacts','A matching, proof-verified terminal Trust Task document may support an exchange claim while its contents stay hidden.','The shared note asks whether this is expressible in the credential model. No implemented extension or task-completion theorem is asserted.','§6 supplies a credential primitive; applying its relations to signed task documents requires an explicit relation and security argument.','Can we prove that a matching, proof-verified terminal document exists without disclosing it? Is this within your model or an extension, and what signatures, transcript bindings and predicates would it require? What does document existence establish about protocol completion, and what external execution facts remain unproved?','An artifact-based relation, authentication requirements and an explicit boundary between a valid terminal record and actual task execution.','Compare credential predicates with exchange-artifact predicates in one worked chapter.'],
['Q24','Can anchoring, status and registry membership share one set-proof interface?','Berkeley 3 · roots and accumulators','A signed published set root and membership or non-membership witness may provide a common interface across several graph checks.','This is the shared note’s proposal; the semantics, authority and freshness of those sets remain distinct.','§5.4 discusses revocation; a unified anchoring/status/registry interface and accumulator choice are DTG design questions.','Does your machinery support a signed published set root with private membership or non-membership proofs, including accumulator non-membership witnesses? Which ZK-friendly constructions are practical on mobile devices? What differs in set authority, root freshness, witness updates, deletion support and availability across anchoring, revocation and trust registries?','A reusable interface with separate set semantics and measured mobile constraints; candidate constructions to evaluate.','Show why a shared cryptographic primitive does not make all sets mean the same thing.'],
['Q25','How is the nullifier secret bound to the credential holder across issuers?','Berkeley 6 · identity/key binding','A bare field element does not itself authenticate a person, signing key or common holder across credentials.','The shared note raises the need for jointly bound dual-issuer secrets rather than combining independent values after issuance.','§7.2 defines PRF and credential relations. Ask which enrollment and issuance relations establish the intended common-holder binding in a DTG profile.','What binds the nullifier secret to an authenticated credential holder or signing key in your framework? Which part depends on enrollment assurance? In the dual- or multi-issuer case, what must be proven jointly at issuance or presentation, and why would combining separately chosen secrets afterward be insufficient?','An exact secret-to-key/credential relation and multi-issuer witness-consistency requirements, without claiming key control alone proves human identity.','Trace enrollment, key control, credential issuance and nullifier derivation as separate claims.'],
['Q26','What agility should we preserve for a post-quantum transition?','Berkeley 7 · security horizon','Long-lived personhood and relationship credentials need an explicit migration policy that preserves their claim semantics.','The shared note asks about migration from the current pre-quantum constructions; no post-quantum security or ready replacement is claimed.','The evaluated instantiations in §10 use elliptic-curve constructions. Migration and long-term privacy need separate analysis.','What migration path do you envisage, and which algorithm identifiers, commitment interfaces, proof profiles and reissuance hooks should we preserve now? Which recorded transcripts or long-lived roots need a separate quantum-threat horizon? Can we retain the semantic claim model while replacing the cryptography?','An agility boundary, credential/reissuance plan and explicitly unresolved long-term privacy assumptions.','Separate the lifetime of the claim from the lifetime of the cryptographic protection.']
];
for(const [id,title,records,statement,evidence,paper,ask,decision,book] of extra)questions.push({id,title,records,statement,evidence,paper,ask,decision,book,section:'Shared Berkeley questions',priority:'Follow-up',url:'https://eprint.iacr.org/2026/333'});
};
