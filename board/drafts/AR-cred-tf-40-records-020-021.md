# AR · cred-tf #40 (reply) — the 24 August decomposition is record 020, with 021 beside it; the unification is 001 + 006; the artifact gap has a home
chip: AUGUST LOOP · THREE SENTENCES · NAMED AFTER YOUR LAST WORD
thread: https://github.com/trustoverip/dtgwg-cred-tf/discussions/40
note: Scott (24 Aug) took stormer78's decomposition (act ∈ scope_n ⊆ … ⊆ scope_root; bounded depth; monotone validity; revocation as non-membership) onto the ZKP list and named the unification (anchoring + revocation + registry membership as one signed-set-root primitive) as ours to pressure-test; the artifact gap flagged. Nobody has said on the thread that these became records.
ledger: 55
proverb: A chain proven link by link is a chain; a chain proven by its root is a set, and the set is what the verifier wanted.
---
The decomposition of 24 August is on the record as construction record 020 (the VDC chain: each link issued by its parent's subject, scopes nested, depth bounded, validity monotone, revocation as non-membership, the root a member of the governing registry) with record 021 beside it for the VAC attenuation chain, both in the ZKP working draft on [`main`](https://trustoverip.github.io/dtgwg-zkp-spec/) at state `specified`. The unification named as ours to pressure-test — anchoring, revocation status and registry membership as one signed set root plus a membership or non-membership proof carried in the presentation — is what records 001 (set membership over an accredited root) and 006 (non-revocation against a status root) are, composed inside 020 and 021; the pressure test is the fixtures those records name, not an argument. The artifact gap now has a home the proof layer can point at: outcome evidence for a cited exchange is proposed in [trust-tasks-spec #15](https://github.com/trustoverip/dtgwg-trust-tasks-spec/pull/15), and the citation itself in committed form is asked in [cred-spec #58](https://github.com/trustoverip/dtgwg-cred-spec/issues/58).

⚔️⊥⿻⊥🧙
