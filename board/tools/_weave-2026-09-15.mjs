// _weave-2026-09-15.mjs — one-off: put the five through-lines and the woven notes into board/survey/contribute.json.
// Source of the text: board/review-notes/CONTRIBUTIONS-2026-09-15.md. Run once; safe to re-run (idempotent by `where`).
import { readFileSync, writeFileSync } from 'node:fs';
import { join, dirname } from 'node:path';
import { fileURLToPath } from 'node:url';
const p = join(dirname(fileURLToPath(import.meta.url)), '..', 'survey', 'contribute.json');
const c = JSON.parse(readFileSync(p, 'utf8'));

c.lines = [
  { id: 'A', title: 'One controller, many identifiers', claim: 'Linkage is proven, never read out of a field; equality of hidden values is a different relation from common control.', records: '007 009 011 012' },
  { id: 'B', title: 'Evidence is not a decision', claim: 'An authenticated statement about a procedure is not the relation a proof establishes, and neither is the admission decision.', records: '008 · 010 negative space · Implementation Guide step 5' },
  { id: 'C', title: 'Bind exactly what you mean', claim: 'A transcript binds a byte-exact predicate identifier and a pinned accept-list revision; nothing is substituted afterwards.', records: '003 · Implementation Guide step 4' },
  { id: 'D', title: 'Authority is a chain, delegation is a chain, and they are not the same chain', claim: 'act ∈ scope_n ⊆ … ⊆ scope_root with bounded depth and per-link status; attenuation is the authority-side sibling.', records: '020 021' },
  { id: 'E', title: 'Continuity survives a key', claim: 'A relationship proof outlives the key that first bound it, through an attested rotation, without the counterparty re-vouching.', records: '004 · exploration X9 · runtimes/guardian-recovery' },
];
c.order = "Close the two August loops first (cred-tf #40, cred-spec #25 — two sentences each), then the byte-input pair (W, AB), then AA where the first proof's user is, then AD, AF and X as the vocabulary threads settle, then AC, AE and the watches. AG after Z, on your read.";

const all = () => [...c.ready, ...c.held, ...c.candidates];
const set = (re, line, note, extra = {}) => {
  const r = all().find(x => re.test(x.draft || '') || re.test(x.where || ''));
  if (!r) { console.log('MISSING', re); return; }
  r.line = line; if (note) r.note = note; Object.assign(r, extra);
};
set(/^U$/, 'A', "The four predicates of 10 September land on 007 (subject-or-issuer common control), 009 (hidden-value equality: a child's issuer equals its parent's subject, across credentials signed by different parties), 020 and 021. Ties to #25 (the shared-subject rule splits the same way) and to #18 (the voucher's linkage is 007 in the voucher's hands, or 009 by reuse).");
set(/^AG$/, 'A', "The chair's question on the offline linkage, answered as the three routes 010 already names: directed reuse (009), issuance-time attestation carried by the VRC (007 in the voucher's hands), the blind-signature vouch (conjecture, from the call). Each gives the two parties asked a record id to answer against.");
set(/trust-tasks-spec PR #6/, 'A', "The task layer's identifierScope (pairwise | public | any) meets the credential layer's declared scope (pairwise | directed | public) on one identifier inside one witnessed exchange. The pantry takes the declared scope as a public input; a proof discloses one scope or says nothing about scope. `any` has no proof meaning. Ask for the mapping to be stated so the scope a proof discloses is the scope the task document records. Ties to htx-tf #11 (where the declaration is made).");
set(/htx-tf #11/, 'A', "A \"face\" per world is a directed identifier per context (011). margeigh's point about minimising decisions has a proof-side reading: the number of identifiers a person keeps is the number of linkage proofs (007) a cross-context presentation needs — fewer faces, cheaper proofs, more correlation — and the UX is where that trade is made.");
set(/cred-spec #53/, 'A', "Declaring nodeType: person establishes no personhood — 002/010's negative space — and a profile that keys policy on nodeType makes it a disclosed public input, fine if said. For agent nodes the property meets line D: an agent's acts trace to a principal through 020/021, not through its nodeType. Ties to general #25 (Q).");
set(/^W$/, 'B', "The vetting statement's identityCommitment is a salted commitment to identity claims — record 008's first route — and not a holder-linkage commitment; livenessConfirmed cannot satisfy a missing membership or voucher-linkage clause. Carry the profile's PASS limits into any private presentation. The card-digest byte-input question belongs here and on vds-spec PR #11.");
set(/vds-spec PR #11/, 'B', "The Vetting Card is the artifact whose digest PR #50 binds. Two asks: (1) the digest is \"taken over the exact form received\" — received bytes or the JCS canonical form? A proof that later binds the digest (003, 008) needs the verifier to reproduce the same bytes. (2) One commitmentSalt per application is the right retention rule — keep it; a reused salt would let two vetters link applications. The eddsa-jcs-2022 proof is the X3 issuance question again (cred-spec #17).");
set(/trust-tasks-tf #8/, 'B', "dcondrey's process-attestation receipt with selectively disclosed fields is record 008's shape applied to an artifact: hide the fields, bind the receipt to the transcript. What it establishes — a captured process produced this artifact — and what it does not — who the creator is, that the process was sound, that the outcome should be accepted — is the negative-space list a record would carry. sankarshan's \"context-bound evidence\" is the right name; the trust task's outcome is the decision, the receipt is evidence for it.");
set(/general #31/, 'B', "ADR-001's use case and, per the 8 September call, the first proof's need; no reply in three weeks. Map the flow's steps to records: membership (001 over an accredited root, 004 holder binding), relationship (010), status (006), with the credential-bound membership presentation (WG-01) as the first deliverable and PR #8 as where the text lives. The one open step is the offline voucher linkage — the same question as #18, so the two threads should cite each other.");
set(/^X$/, 'C', "Two things the request profile needs from the registry: the identifier comparison rule (byte-exact IRI, RFC 3987 §3.1 as talltree proposed) and the pinned accept-list revision, both bound into the transcript before construction (Implementation Guide step 4). A valid digest does not make an unknown predicate meaningful; no alias or version substitution after binding.");
set(/PR #54/, 'C', "The ZKP draft follows the convention once #54 merges: _Version:_ 1.0, _Document Status:_ Working Draft 0.1.0; the cross-specification \"minimum Document Status\" line is exactly what the [DTG-CRED] reference will carry.");
set(/cred-spec #51 /, 'C', "One line of assent, or silence — the header follows once #54 merges.");
set(/general #32/, 'D', "swatchen's dual-path actuation and sankarshan's \"non-collapsibility of control\" split exactly as 020 (delegation: who may act for whom) and 021 (authority: what the act may be, attenuated down the chain). A proof carries the authority path; capability enforcement is local and not a proof concern. Reply only if it moves toward the credential layer.");
set(/^Q$/, 'D', "A VMC issued to an agent node establishes membership under the community's rules, not agenthood, and an agent's acts trace to a principal through 020/021. The agents-only VTC twin is the worked example. One disclosure at a time; post when the mages.city lane is ready to be cited.");
set(/general #23/, 'E', "The question — who inherits a relationship when a person dies or is incapacitated — has a worked proof-side answer in exploration X9 and runtimes/guardian-recovery (12/12): guardian attestations that expire and must be re-affirmed, issuer-blind reissuance, and the counterparty's edges either reforming or continuing under an attested rotation of the holder key (004). adligo's custodial-wallet suggestion is a custody-model choice outside the proof; what no proof establishes is legal authority — the assurance boundary. Ties to line A: the rotation must preserve common control (007) or the relationship proofs break.");
set(/^D$/, 'D', "Nothing further unless the Projects board appears.");

const add = (item) => { if (!c.candidates.some(x => x.url === item.url)) c.candidates.push(item); };
add({ where: 'cred-spec #25 — Relationship policy discovery: both communities must admit an edge (stormer78; geoffturk 7 Sept: the vocabulary moved, nothing merged answers the three questions)', url: 'https://github.com/trustoverip/dtgwg-cred-spec/issues/25', why: 'Record 013 (mutual edge admissibility, requested) is the answer on the branch.', note: "Mutual edge admissibility is carded as record 013 at requested — the commit-before-reveal handshake noted on 25 August, stated as a predicate: both communities' admissibility policies accept the edge form, proven to each side without revealing membership in the other. Step one stays plain published fields, no proof machinery; the record exists so the proof-shaped step has an identifier when it is wanted. Ties to #52 and #48: the admissibility predicate is itself a registry term.", records: '013 007 012', category: 'reply', action: 'two sentences: 013 exists on the branch; step one stays published fields', line: 'A' });
add({ where: 'cred-tf #40 — Delegation as a case study in the design-time window (stormer78; Scott 24 Aug: delegation-chain validity is the cleaner ZKP target, with the decomposition)', url: 'https://github.com/trustoverip/dtgwg-cred-tf/discussions/40', why: "Scott's decomposition is now record 020; the thread has waited since August.", note: "Scott's 24 August decomposition — act ∈ scope_n ⊆ … ⊆ scope_root, bounded depth, monotone validity, revocation as non-membership — is now record 020 on the branch, with 021 as the attenuation sibling. sankarshan's preference for verification without verifier-originated live lookups is 006's set-root convention: roots and witnesses fetched anonymously per epoch. Two sentences close the thread.", records: '020 021 006', category: 'reply', action: 'two sentences closing the loop: 020/021 on the branch; 006 answers the live-lookup point', line: 'D' });
add({ where: 'cred-spec #48 — Decide the home of the DTG namespace before the Implementers Draft (geoffturk; 13 Sept constraints from #52; albertoleon 14 Sept: …/dtg/v1 already deployed)', url: 'https://github.com/trustoverip/dtgwg-cred-spec/issues/48', why: 'Only two namespace properties matter to a proof; a namespace change is a re-issuance event.', note: "From the proof side only two properties of the namespace matter: term immutability (a bound IRI must mean the same thing for the life of every transcript that binds it) and byte-exact comparability. Ownership is not a proof concern. albertoleon's note that …/dtg/v1 is already deployed has a proof-layer consequence worth saying: changing the namespace changes every bound predicate IRI, which is a re-issuance event of the kind the call called non-swappable.", records: '003', category: 'discuss', action: 'one comment: immutability + byte-exact comparability; a namespace change = re-issuance', line: 'C' });

writeFileSync(p, JSON.stringify(c, null, 2) + '\n');
console.log('lines', c.lines.length, '· candidates', c.candidates.length, '· items with a line:', all().filter(x => x.line).length, '· without:', all().filter(x => !x.line).map(x => x.draft || x.where.slice(0, 30)).join(' | '));
