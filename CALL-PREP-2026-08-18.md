# CALL PREP — 2026-08-18 (v0.4 adoption path on today's agenda)

Scott's 08-17 comment on discussion #13 ends: *"I will put it on tomorrow's agenda"* — that is
**today**. The item: how Sankarshan's v0.4.0 draft comes upstream.

## 1. What moved upstream since 08-14 (survey, 2026-08-18)

- **Discussion #13 (v0.4)** — Sankarshan wrote a **full v0.4.0 requirements draft in his fork**
  (606 lines, stable `LIV-*` requirement ids, MLP/EPP profile model, three freshness clocks,
  biometric-provider input contract §14, verification-outcome contract §15, decision backlog §19):
  https://github.com/sankarshanmukhopadhyay/dtgwg-zkp-tf/blob/main/proof-of-liveness-requirements.md
  His document model: **requirements doc = semantic source of truth** for predicates + paired
  boundaries; **the decision doc operationalises**; **the predicate register records
  implementation-level boundaries**. Scott (08-17): strong support, affirms the assurance
  boundary (LIV-CLAIM-02), §14, and the freshness clocks; defers construction-level to
  Denys/Nicholas/Berkeley; asks the adoption-path question (whole doc as official v0.4 vs
  semantic core upstream + implementation machinery stays in his implementation-guide tree).
- **Discussion #12 (Scott, 08-05, 0 comments — unanswered)** — he *ran the research cycle on the
  lab and walked the path map*, then filed two **refine-and-build positions under his own name**:
  (a) ratify the "scoped reuse detection" narrowing but make loud that anti-Sybil force comes
  from **dedup enrolment quality** (FMR/FNMR, re-enrolment resistance, capture conditions) —
  offers a biometric-provider write-up for the enrolment-root discussion; (b) X8 dependency
  classes need a **biometric-specific axis** (shared model / shared vendor / shared enrolment
  data) — separate legal entities are not independent issuers when they share a model.
- **Issue #1 (public repo, Scott 08-17)** — warm close of the loop: "the process survived contact
  with its first stranger and held without changing… the strongest answer to 'is this real'";
  names the G.1 rite as *"genuinely the PR-LIV shape applied to your own seat"*; **commits to
  co-shaping Lane 2 live-human-entropy with Realeyes** when it opens. Standing thread, stays open.
- Upstream repo commits: none since `b37d52f` (clone synced clean). No new verification-run
  issues on the public repo.

## 2. The evidence hook worth saying on the call

Draft §16.1 + backlog line 560 hold an **open evidence gate**: experimental circuit benchmarks
"MUST NOT by themselves support readiness claims **until independently reproduced or otherwise
independently verified**."

**The registry already satisfies the reproduction half**: entry 0006-gg-affinidi = fully external
run (Affinidi, darwin/arm64, Apple M5 Max), required digests **byte-identical cross-OS and
cross-architecture** to the win32/x64 manifest, all suites green, acceptance model naming every
advisory divergence. One sentence for the call: *the independent-verification requirement the
draft imposes on circuit evidence is already met for reproduction-of-build-and-behavior — what
remains open, correctly, is third-party review of the statements themselves, which is exactly the
narrowness CIRCUITS.md claims.* Offer the registry row + ACCEPTANCE-FLOW.md as the gate's
evidence format.

## 3. Substrate pivot (new note, feeds items 5 and 6 of #13)

`explorations/NOTE-2026-08-18-ef-substrate-pivot.md` — EF pivots L1 to hash-friendly SNARKs
(Binius / Flock; Drake 08-13; no Poseidon break, L1-scope only). Plugs directly into:

- **Draft LIV-ALG-07** (PQ migration readiness: "migration triggers"): the EF pivot is a live
  example of a migration trigger firing **strategically, not cryptanalytically** — supports the
  agility-mandatory posture and gives §10's assurance-horizon language a real-world citation.
- **§16.1 / construction selection**: substrate (prime vs binary field) = a selection axis to
  declare; lab constraint counts = era-specific; transcript-binding (+1 constraint) = the
  portable finding.
- **Register caution** (carried in the note): HAWK/SQIsign "blows" = hearsay, keep out of TF
  artefacts.

Two-comment posting shape (short gated + detail self-reply) drafted in the note's disposition;
target = #13 (items 5/6) rather than a new thread. Posting = you.

## 4. Reply candidates (all = your call, in priority order)

1. **#13**: support the Sankarshan document model + the §2 evidence-gate sentence + substrate
   note as input to items 5/6. (If adoption path is decided live, the comment can follow the call.)
2. **#12**: accept both refine-and-builds — (a) invite the enrolment-dedup write-up as the
   enrolment-root-in-preimage discussion's missing biometric half; (b) fold the biometric
   dependency axis into X8's independence register (`sharedModel`/`sharedVendor`/`sharedEnrolmentData`
   as dependency classes — small executable change, can be built same-day as evidence).
3. **Issue #1**: optional ack of the Lane 2 commitment; or fold into the Lane 2 seam when it opens.

## 5. Local state (the sync ledger)

- **Research root, uncommitted**: KB rebuild (X10 + X11 manifested, 30 kb/ files modified),
  `explorations/X11-field-guide-deployment.md` (untracked), INTEGRATION-MAP.md, build-kb.mjs,
  substrate note (new), this file. Commit + push = you (G.1 rite when pushing).
- **Workbench**: clean at upstream `b37d52f`; only the call companion html untracked.
- **Chronicle 7 written** (master series): `2026-08-18_the-clock-ticks-without-a-break.md` —
  inversion: *the horizon moves before the mathematics*. Unsigned; public reflection into
  `chronicles/` waits on your read per the signed-before-public gate.
- X10 Lane 2 claim narrowing ("survives §25 either way" → "within the BN254-compatible set")
  still to apply in the X10 doc + CEREMONY-PROPOSAL-DRAFT before circulation.
