# REVIEW — the book and the pushes, in full (2026-09-05)

*How to read everything that is staged, in the order that makes the commits reviewable. Nothing below
is committed, posted or pushed; every item is yours to accept, change or refuse.*

## 1. Read the book

| what | where | note |
|---|---|---|
| the specification as the spec repository renders it | http://127.0.0.1:8427/ | rendered from the clone `~/dtgwg-zkp-spec` (branch `zk-book`) with the repository's own Spec-Up-T config; this is what the PR would publish |
| the same records as the evidence-repository edition (the ZK Book) | http://127.0.0.1:8426/ | multi-file chapters, identical content |
| the reader — drafts (H P G F J K L M · D I), doors, watch, cards | http://127.0.0.1:8425/ | each draft has its proverb gate; copy opens after Activate |
| the chapter sources | `zkbook/spec/*.md` | hand-written: header · intro · terms intro · pantry · considerations · conformance · references · appendix; generated: records · primer · recipes · stacks · privacy-derived · terms `g-*` |
| the records behind the generated text | `board/cards/*.json` · `board/records/ADR-001.json` · `board/stacks/*.json` | change these, never the generated text; `node board/tools/board.mjs spec` regenerates |

Reading order for a first pass: header → intro → Requests Answered (ADR-001 crosswalk) → one primitive
construction (001) → one composed (010) → Proving Systems → Conformance → References/Appendix A.

## 2. Read the pushes — the three commits

Run first, from `~/dtgwg-zkp-spec`:

```
git status                       # 37 paths: 9 modified/deleted, 28 untracked
git diff --stat                  # the template files this replaces
node conformance/test.mjs        # 17/17 valid · body.md stamp current
npm run render                   # docs/index.html (never committed)
```

### C1 — `spec: title, front matter and repository description for the DTG ZKP specification`

```
git add specs.json README.md spec/header.md spec/intro.md spec/terms-and-definitions-intro.md spec/appendix.md
git rm spec/terms-definitions/verifiable_credential.md spec/terms-definitions/verifiable_trust_community.md
```

Review: `spec/header.md` — version 0.1, status Working Draft, **editors line** (you; Scott under
contributors — confirm), abstract, IPR block per the template with "Decentralized Trust Graph Working
Group". `spec/intro.md` — the record form, the state table, the split, Requirements Language.
`spec/appendix.md` — Appendix A provenance incl. the **relicensing statement placeholder** (C3 waits for
your sentence), Appendix B the evidence repository, C acknowledgements, D changelog. `README.md` — what
the repo is, the three-repositories table, the ZK Book edition paragraph. `specs.json` — only title,
description, author and the `DTG_CRED` external spec changed; their logo/favicon/source/paths kept.

### C2 — `conformance: machine-readable records, validator and CI`

```
git add conformance .github/workflows/validate-conformance.yml
```

Review: `conformance/records/*.json` (12) — the construction records; `conformance/requests/ADR-001.json`;
`conformance/stacks/*.json` (4: flock · lab-groth16-circom · provekit · siros-longfellow);
`conformance/schema/construction-record.schema.json`; `conformance/validate.mjs` — the rules, read the
register strings; `conformance/test.mjs` — the digest stamp check; `conformance/README.md`;
`.github/workflows/validate-conformance.yml` — pinned action SHAs match the repository's own workflows.

### C3 — `spec: body — requests, background, public inputs, construction records, proving systems, considerations, conformance, references`

```
git add spec/body.md spec/terms-definitions/
```

Review: `spec/body.md` (≈3,000 lines) — the stamp on line 1 must equal `node conformance/test.mjs`'s
digest; the section order is Requests Answered → Cryptographic Background → Public Inputs → Construction
Records → Proving Systems → Security → Privacy (editors' items then the generated list) → Governance →
Internationalization → Accessibility → Conformance → References. Twenty-six `g-*` terms.

Each commit: `node tools/push-rite.mjs serve … → speak <seq> → footer <seq>` in the evidence repository,
paste the footer above `Signed-off-by`, `git commit -s`, no AI trailer.

### Before C1

- Confirm the editors line with Scott.
- Write the relicensing sentence into Appendix A (the Cryptographic Background is adapted from the
  spellbook's explanatory material; CC BY 4.0 for this specification is your act as author).

## 3. Read the posts

Order: **H** (zkp-tf new discussion — the anchor) → **P** (the PR body, once `zk-book` is pushed and the
compare link opens) → **M** (#14 — three repositories, the book edition) → **G** (#18) → **F** (#9) →
**J** (#31) → **K** (#39, before 8 Sept) → **L** (#17). **I** optional. **D** waits for the Projects board.
Each is in `board/drafts/`; the reader shows them in file order with chips saying where they sit.

## 4. What is deliberately not in the pushes

- The options layer (stacks × constructions × the §25 gate as fields, the reproduction ladder, the
  Recommendations chapter) — `PLAN-options-layer.md`; second pull request.
- Any measured Flock, ProveKit or SIROS row — all conjecture-labelled until a run exists.
- Zero Tale 32 and the grimoire patch — spellbook side, not specification side.

## 5. Checks that must stay green

`node board/test.mjs` (47) · `node conformance/test.mjs` in the clone · `npm run render` in the clone ·
`node tools/zkbook-export.mjs --check` reporting nothing changed after a regenerate.
