# The second commit on `zk-book` — the push that makes the queue citable (prepared 2026-09-14)

*Your act. Nothing below runs itself. Every check named here was green on 14 September; run them again first.*

## Why this comes first

PR #8 holds one commit (`91b0e8c`, 8 September). Every draft in the queue — U (cred-spec #9), T (#23), V (#23),
W (PR #50), X (#52) — cites records 009 / 013 / 021, the revised 003 / 007 / 008 / 010 / 020, the `hidden-equality`
gadget or Appendix E. None of that is public. The 12 September wording hedges every one of them ("local review
candidates … not yet on the public PR"). One push removes the hedge from all five.

## What the commit carries (clone `~/dtgwg-zkp-spec`, branch `zk-book`)

18 modified · 4 new · +563 −55:

| group | files |
|---|---|
| records revised | `conformance/records/003.json` (predicate identity, #52) · `007.json` (subject-or-issuer; chain predicates carved out, #9) · `008.json` (PR #50 commitment as route 1) · `010.json` (blind-signature vouch; vetting ≠ voucher linkage) · `020.json` (merged VDC re-read) |
| records new | `009.json` hidden-value equality · `013.json` mutual admissibility (`requested`) · `021.json` VAC attenuation chain |
| gadget | `conformance/schema/construction-record.schema.json` · `validate.mjs` · `render-lib.mjs` · the four `stacks/*.json` coverage rows · `spec/terms-definitions/g-gadget-hidden-equality.md` |
| chapters | `spec/intro.md` · `implementation-guide.md` (steps 4 and 5) · `integration.md` · `research-and-book.md` (your 10 Sept research notes + the 13 and 14 Sept subsections) · `appendix.md` (changelog, Appendix E) · `body.md` (generated + Security §5, i18n, conformance count, references) |

## Commands

```
cd ~/dtgwg-zkp-spec
git status --short                       # expect the 22 paths above and nothing else
node conformance/validate.mjs            # 20/20 valid
node conformance/test.mjs                # all conformance checks pass
npm run render                           # docs/index.html (never committed)
git add conformance spec
git commit -s -F ~/dtgwg-cred-spec-main_mage/zkbook/COMMIT-2026-09-14.msg
git log -1 --format='%B'                 # read it once more: trailers, sign-off, no AI trailer
git push origin zk-book
```

The clone signs off as `mitchuski <mitchuski@users.noreply.github.com>` — that is you; leave it.

## The gate

The first commit carried `Review-Receipt-SHA256` + `Review-Proverb` trailers from the reader's revision receipt rather
than the push rite's `Ledger-Head/Seq/Domain` footer. Ledger **39** is served as a `push` entry for this commit so the
rite is available if you rule that way (door D22); the `.msg` below carries the proverb as a `Review-Proverb` trailer to
match the first commit, and leaves the receipt line for you to fill from the reader if you use it. Either way: DCO
sign-off, no AI co-author trailer.

## After the push

1. Post **Y** on PR #8 (ledger 40) — five lines saying what the second commit is.
2. Then the queue reads without hedges: U → T → mark PR #8 ready for review → V; W after #47/#50; X once #52 shapes.
3. The reader's watch on `dtgwg-zkp-spec` picks the new head up on the next survey.
