# 2026-08-18 — r1: the spec draft folds 980 → 863, census 40/40

**Verdict first.** The first round of the cred-spec lane's compression
instance produced two VALIDATED levers against the frozen 40-fact census —
`line-edit-pass-1` (940) and `restructure-telegraphic-tables` (863), both
40/40 with detection 1.0 — and the keystone folded the best: **980 → 863
(−11.9%)**, OT-1 MET, OT-2 open (compose a line-edit over the fold; the
dominated 940 family is the standing fallback). The lane's original
`SPEC-DRAFT-proof-of-understanding-trust-task.md` is untouched — promoting
the fold up to it is the First Person's door.

**What the round was.** Census mode over census.json (F1..F40,
keystone-enumerated from the original — the u2/logos lesson: a frozen list,
never a rule two seats could read differently). Both proposers stayed blind
to the census; the Gap saved canon bytes whose sha256 IS the seed (legacy
unsalted mode — no run secret was passed); the assay answered all forty
questions from the candidate alone and graded against the census text.
Keystone audit at fold time: `verify_run` RUN VERIFIED, seeds re-derived;
spot-grep of the fragile facts (the five error codes, `actsAsSubject:
false`, the mage.mesh non-canonisation, the NOT-upstreamed status labels)
all present in the 863 candidate; word count re-derived 863 by the counting
rule.

**Two findings at the harness level, filed upstream.**
1. **Defect #13 (found by this fold):** `verify_run.mjs`'s legacy path
   replayed a seed-derived 8-sample against a census draw and cried
   UNVERIFIABLE on an honest round — a census has no subset to replay.
   Fixed in the harness repo; the run then verified.
2. **Salt note:** this round ran unsalted (no `saltSecret` in the args).
   Under a census there is no draw to grind, so the gate's strength is
   unaffected — but future rounds should pass a salt anyway for uniform
   provenance (`seed = sha256(hSource‖hProposal‖salt)`).

**INCOMPLETE, honestly.** The engine reports the run INCOMPLETE because
round r1.2's proposers (and the r1.2 chronicle seat) died on a session
limit after r1.1 completed clean through its critic. Nothing from r1.2
existed to fold; the fold above consumes only r1.1's confirmed levers.

**Handoff.** OT-2 open (compose ~sub-863). Doors (the First Person's):
promote the 863 fold to the lane root; any upstream sharing rides the
lane's own gates. Next action: an OT-2 compose round, salted.
